@model WebAppProject.Models.Category

@{
    ViewBag.Title = "Delete";
}

<h2>Excluir</h2>

<h3>Atenção!</h3>
<div>
    <h4>Category</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NomeCategoria)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NomeCategoria)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            @Html.ActionLink(" ", "Delete", new { id = Model.CategoriaId }, new { onclick =
                "return confirm ('Você tem certeza que quer retirar este item?);",
                @class = "btn btn-danger glyphicon glyphicon-trash"})
            @Html.ActionLink(" ", "Index", null, htmlAttributes: new { @class = "btn btn-default glyphicon glyphicon-backward" })
        </div>
    }
</div>
